# Running the project
Please create a `.env` file in `../frontend/.env` with the following contents:

```
PUBLIC_API_BASE_URL=http://localhost:5110

```
```
```
After which you may launch the react project with the following commands:
```
npm install
npm run dev
```

The backend project remains unchanged

```
dotnet restore
dotnet run
```

